node {
 
  stage 'sudo Docker build'
  docker.build('test')
 
  stage 'sudo Docker push'
  docker.withRegistry('https://743148602960.dkr.ecr.ap-south-1.amazonaws.com/test', 'ecr:ap-south-1:test-ecr-credentials') {
    docker.image('test').push('latest')
  }
}
